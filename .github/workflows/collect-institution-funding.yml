name: 수집 — Data.go 기관별 수신잔액

on:
  workflow_dispatch:
    inputs:
      password:
        description: "관리자 수집 암호"
        required: true
        default: ""
      mode:
        description: "watch=일상 갱신 감시 / backfill=명시적 과거 적재"
        type: choice
        required: true
        default: watch
        options:
          - watch
          - backfill
      periods:
        description: "watch 모드의 업권별 최근 보고기간 수"
        type: number
        required: false
        default: 12
      start_month:
        description: "backfill 시작월 YYYYMM"
        required: false
        default: "201801"
      end_month:
        description: "backfill 종료월 YYYYMM (비우면 실행월)"
        required: false
        default: ""

  schedule:
    - cron: "52 15 * * 0-4"

concurrency:
  group: rate-data-writer
  queue: max
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Check collect password
        if: ${{ github.event_name == 'workflow_dispatch' }}
        env:
          EXPECTED: ${{ secrets.DASHBOARD_PASSWORD }}
          GIVEN: ${{ inputs.password }}
        run: |
          set -euo pipefail
          test -n "$EXPECTED"
          test "$GIVEN" = "$EXPECTED"

      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync dependencies
        run: uv sync --locked

      - name: Require authoritative R2 backend
        env:
          STORAGE_BACKEND: ${{ vars.STORAGE_BACKEND }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ vars.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          R2_ENDPOINT: ${{ vars.R2_ENDPOINT }}
          R2_REGION: ${{ vars.R2_REGION }}
        run: |
          set -euo pipefail
          BACKEND=$(uv run python - <<'PY'
          from pathlib import Path
          from rate_monitor.services.storage_service import load_backend
          print(load_backend(Path("config/storage.yaml")).backend.value)
          PY
          )
          test "$BACKEND" = "r2"
          echo "authoritative backend=$BACKEND"

      - name: Restore authoritative R2 database
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ vars.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          R2_ENDPOINT: ${{ vars.R2_ENDPOINT }}
          R2_REGION: ${{ vars.R2_REGION }}
        run: |
          set -euo pipefail
          mkdir -p work publish data/raw
          uv run rate-monitor storage restore --dest work/rate_monitor.sqlite3

      - name: Apply migrations
        env:
          RATE_MONITOR_DB_URL: sqlite+pysqlite:///${{ github.workspace }}/work/rate_monitor.sqlite3
        run: uv run alembic upgrade head

      - name: Refresh ECOS funding-sector totals
        env:
          ECOS_API_KEY: ${{ secrets.ECOS_API_KEY }}
        run: |
          uv run python -m rate_monitor.collectors.bok_ecos.macro_cli \
            --db work/rate_monitor.sqlite3 \
            --raw-root data/raw \
            --json publish/ecos-funding-run.json

      - name: Resolve Data.go collection window
        id: window
        env:
          MODE: ${{ inputs.mode || 'watch' }}
          PERIODS: ${{ inputs.periods || 12 }}
          START_MONTH: ${{ inputs.start_month || '201801' }}
          END_MONTH: ${{ inputs.end_month }}
        run: |
          set -euo pipefail
          if [[ "$MODE" == "backfill" ]]; then
            if [[ -z "$END_MONTH" ]]; then END_MONTH=$(date -u +%Y%m); fi
            echo "mode=backfill" >> "$GITHUB_OUTPUT"
            echo "start_month=$START_MONTH" >> "$GITHUB_OUTPUT"
            echo "end_month=$END_MONTH" >> "$GITHUB_OUTPUT"
            echo "periods=" >> "$GITHUB_OUTPUT"
          else
            echo "mode=watch" >> "$GITHUB_OUTPUT"
            echo "start_month=" >> "$GITHUB_OUTPUT"
            echo "end_month=" >> "$GITHUB_OUTPUT"
            echo "periods=$PERIODS" >> "$GITHUB_OUTPUT"
          fi

      - name: Collect Data.go institution funding
        env:
          DATA_GO_KR_SERVICE_KEY_SB: ${{ secrets.DATA_GO_KR_SERVICE_KEY_SB }}
          DATA_GO_KR_SERVICE_KEY_SH: ${{ secrets.DATA_GO_KR_SERVICE_KEY_SH }}
          DATA_GO_KR_SERVICE_KEY_NH: ${{ secrets.DATA_GO_KR_SERVICE_KEY_NH }}
          DATA_GO_KR_SERVICE_KEY: ${{ secrets.DATA_GO_KR_SERVICE_KEY }}
          MODE: ${{ steps.window.outputs.mode }}
          PERIODS: ${{ steps.window.outputs.periods }}
          START_MONTH: ${{ steps.window.outputs.start_month }}
          END_MONTH: ${{ steps.window.outputs.end_month }}
        run: |
          set -euo pipefail
          ARGS=(--db work/rate_monitor.sqlite3 --raw-root data/raw)
          if [[ "$MODE" == "backfill" ]]; then
            ARGS+=(--start-month "$START_MONTH" --end-month "$END_MONTH")
          else
            ARGS+=(--periods "$PERIODS")
          fi
          uv run python -m rate_monitor.collectors.data_go_funding.cli collect \
            "${ARGS[@]}" --json publish/funding-first-run.json

      - name: SQL readback, continuity and reconciliation
        run: |
          set -euo pipefail
          uv run python -m rate_monitor.collectors.data_go_funding.cli report \
            --db work/rate_monitor.sqlite3 \
            --json publish/funding-report.json
          uv run python -m rate_monitor.collectors.data_go_funding.cli audit \
            --db work/rate_monitor.sqlite3 \
            --windows 24,36 \
            --json publish/funding-history-audit.json
          uv run python - <<'PY'
          import json, sqlite3
          c = sqlite3.connect("work/rate_monitor.sqlite3")
          c.row_factory = sqlite3.Row
          total = c.execute(
              "SELECT COUNT(*) FROM institution_funding_observations"
          ).fetchone()[0]
          active = c.execute(
              "SELECT COUNT(*) FROM institution_funding_observations WHERE valid_to IS NULL"
          ).fetchone()[0]
          sources = c.execute(
              "SELECT source_id, COUNT(*) n "
              "FROM institution_funding_observations WHERE valid_to IS NULL "
              "GROUP BY source_id ORDER BY source_id"
          ).fetchall()
          assert total > 0 and active > 0
          assert any(r["source_id"] == "data_go_savings_bank_funding" and r["n"] > 0 for r in sources)
          assert any(r["source_id"] == "data_go_agri_coop_funding" and r["n"] > 0 for r in sources)
          payload = {
              "total": total,
              "active": active,
              "sources": [dict(r) for r in sources],
              "integrity_check": c.execute("PRAGMA integrity_check").fetchone()[0],
              "foreign_key_violations": len(c.execute("PRAGMA foreign_key_check").fetchall()),
          }
          assert payload["integrity_check"] == "ok"
          assert payload["foreign_key_violations"] == 0
          open("publish/funding-readback.json", "w", encoding="utf-8").write(
              json.dumps(payload, ensure_ascii=False, indent=2) + "\n"
          )
          print(json.dumps(payload, ensure_ascii=False))
          PY
          uv run python -m rate_monitor.collectors.data_go_funding.cli counts \
            --db work/rate_monitor.sqlite3 > work/funding-before-rerun.json

      - name: Detect semantic data change
        id: changes
        run: |
          set -euo pipefail
          uv run python - <<'PY'
          import json, os
          ecos = json.load(open("publish/ecos-funding-run.json"))
          funding = json.load(open("publish/funding-first-run.json"))
          changed = int(ecos.get("stored", 0)) > 0 or any(
              int(row.get("stored", 0)) > 0 or int(row.get("revisions", 0)) > 0
              for row in funding
          )
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
              out.write(f"changed={'true' if changed else 'false'}\n")
          print("semantic data changed:", changed)
          PY

      - name: Idempotent rerun
        env:
          DATA_GO_KR_SERVICE_KEY_SB: ${{ secrets.DATA_GO_KR_SERVICE_KEY_SB }}
          DATA_GO_KR_SERVICE_KEY_SH: ${{ secrets.DATA_GO_KR_SERVICE_KEY_SH }}
          DATA_GO_KR_SERVICE_KEY_NH: ${{ secrets.DATA_GO_KR_SERVICE_KEY_NH }}
          DATA_GO_KR_SERVICE_KEY: ${{ secrets.DATA_GO_KR_SERVICE_KEY }}
          MODE: ${{ steps.window.outputs.mode }}
          PERIODS: ${{ steps.window.outputs.periods }}
          START_MONTH: ${{ steps.window.outputs.start_month }}
          END_MONTH: ${{ steps.window.outputs.end_month }}
        run: |
          set -euo pipefail
          ARGS=(--db work/rate_monitor.sqlite3 --raw-root data/raw)
          if [[ "$MODE" == "backfill" ]]; then
            ARGS+=(--start-month "$START_MONTH" --end-month "$END_MONTH")
          else
            ARGS+=(--periods "$PERIODS")
          fi
          uv run python -m rate_monitor.collectors.data_go_funding.cli collect \
            "${ARGS[@]}" --json publish/funding-second-run.json
          uv run python -m rate_monitor.collectors.data_go_funding.cli counts \
            --db work/rate_monitor.sqlite3 > work/funding-after-rerun.json
          uv run python - <<'PY'
          import json
          before = json.load(open("work/funding-before-rerun.json"))
          after = json.load(open("work/funding-after-rerun.json"))
          if before != after:
              raise SystemExit(f"funding observation duplicate/revision drift: {before} -> {after}")
          print("idempotency ok:", after)
          PY

      - name: Snapshot and validate changed state
        if: ${{ steps.changes.outputs.changed == 'true' }}
        run: |
          set -euo pipefail
          uv run rate-monitor snapshot \
            --db work/rate_monitor.sqlite3 \
            --publish-db publish/rate_monitor.sqlite3 \
            --manifest publish/manifest.json
          uv run rate-monitor validate --db publish/rate_monitor.sqlite3

      - name: Upload changed authoritative state to R2
        if: ${{ steps.changes.outputs.changed == 'true' }}
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ vars.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          R2_ENDPOINT: ${{ vars.R2_ENDPOINT }}
          R2_REGION: ${{ vars.R2_REGION }}
        run: uv run rate-monitor storage upload --db publish/rate_monitor.sqlite3

      - name: Verify changed R2 restore byte-for-byte
        if: ${{ steps.changes.outputs.changed == 'true' }}
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ vars.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          R2_ENDPOINT: ${{ vars.R2_ENDPOINT }}
          R2_REGION: ${{ vars.R2_REGION }}
        run: |
          set -euo pipefail
          uv run rate-monitor storage restore --dest work/funding-from-r2.sqlite3
          cmp -s publish/rate_monitor.sqlite3 work/funding-from-r2.sqlite3
          uv run python - <<'PY'
          import sqlite3
          c = sqlite3.connect("work/funding-from-r2.sqlite3")
          assert c.execute("PRAGMA integrity_check").fetchone()[0] == "ok"
          assert not c.execute("PRAGMA foreign_key_check").fetchall()
          n = c.execute(
              "SELECT COUNT(*) FROM institution_funding_observations WHERE valid_to IS NULL"
          ).fetchone()[0]
          assert n > 0
          print("R2 funding readback active rows:", n)
          PY

      - name: No-change R2 decision
        if: ${{ steps.changes.outputs.changed != 'true' }}
        run: echo "No semantic ECOS/Data.go change; authoritative R2 snapshot remains unchanged."

      - name: Upload funding evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: institution-funding-${{ github.run_id }}
          retention-days: 90
          if-no-files-found: warn
          path: |
            data/raw/
            publish/ecos-funding-run.json
            publish/funding-first-run.json
            publish/funding-second-run.json
            publish/funding-report.json
            publish/funding-history-audit.json
            publish/funding-readback.json
            publish/manifest.json
