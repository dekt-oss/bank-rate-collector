name: Maintenance — apply CU historical quarantine patch

on:
  push:
    branches:
      - feat/cu-funding-disclosure-20260829
    paths:
      - .github/workflows/patch-cu-funding-historical-quarantine.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/cu-funding-disclosure-20260829

      - name: Apply patch and remove temporary patch machinery
        run: |
          set -euo pipefail
          python scripts/patch_cu_funding_historical_quarantine.py
          rm -f .github/workflows/patch-cu-funding-historical-quarantine.yml
          rm -f scripts/patch_cu_funding_historical_quarantine.py
          rm -f scripts/patch_cu_funding_source_contract.py
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "fix: quarantine unverifiable historical CU disclosures"
          git push origin HEAD:feat/cu-funding-disclosure-20260829
