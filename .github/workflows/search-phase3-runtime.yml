name: Search phase 3 runtime

on:
  push:
    branches: ["feat/search-ux-phase3-closure-*"]
    paths:
      - "web/templates/site.html"
      - "scripts/search_phase3_runtime.js"
      - "tests/test_search_phase3_closure_contract.py"
      - "docs/specs/20260824-search-ux-phase3-closure-v1.md"
      - ".github/workflows/search-phase3-runtime.yml"
      - "src/rate_monitor/services/site_service.py"
      - "src/rate_monitor/services/main_map_presentation.py"
      - "src/rate_monitor/services/dashboard_ui_refinement_presentation.py"
      - "config/**"
      - "migrations/**"
      - "pyproject.toml"
      - "uv.lock"
  workflow_dispatch:

concurrency:
  group: search-phase3-runtime
  cancel-in-progress: true

permissions:
  contents: read
  statuses: write

jobs:
  runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Publish Search phase 3 runtime locator
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          gh api --method POST "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
            -f state=pending \
            -f context=search-phase3-runtime \
            -f description="Search phase 3 runtime running" \
            -f target_url="$TARGET_URL"

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync dependencies
        run: uv sync --locked

      - name: Run Search phase 3 contract tests
        run: uv run pytest -q tests/test_search_phase3_closure_contract.py

      - name: Restore production database to runner-local copy
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ vars.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          R2_ENDPOINT: ${{ vars.R2_ENDPOINT }}
          R2_REGION: ${{ vars.R2_REGION }}
        run: |
          set -euo pipefail
          mkdir -p work
          uv run rate-monitor storage restore --dest work/search-phase3.sqlite3
          test -s work/search-phase3.sqlite3

      - name: Apply migrations to runner-local copy
        env:
          RATE_MONITOR_DB_URL: sqlite+pysqlite:///${{ github.workspace }}/work/search-phase3.sqlite3
        run: uv run alembic upgrade head

      - name: Seal runner-local database
        run: |
          set -euo pipefail
          sha256sum work/search-phase3.sqlite3 | awk '{print $1}' > work/search-phase3-db-before.sha256
          cat work/search-phase3-db-before.sha256

      - name: Build Search with Strategy Gate OFF
        env:
          RATE_MONITOR_STRATEGY_DASHBOARD: "0"
        run: |
          set -euo pipefail
          rm -rf published/search-phase3
          uv run rate-monitor build-site \
            --db work/search-phase3.sqlite3 \
            --out published/search-phase3 \
            --export-dir work/no-search-phase3-exports
          test -s published/search-phase3/index.html
          test -s published/search-phase3/data/table.json
          test ! -e published/search-phase3/strategy.html

      - name: Assert build did not mutate DB
        run: |
          set -euo pipefail
          BEFORE=$(cat work/search-phase3-db-before.sha256)
          AFTER=$(sha256sum work/search-phase3.sqlite3 | awk '{print $1}')
          echo "db before=$BEFORE"
          echo "db after =$AFTER"
          test "$BEFORE" = "$AFTER"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Playwright runner
        run: |
          set -euo pipefail
          command -v google-chrome
          google-chrome --version
          npm install --no-save --package-lock=false @playwright/test@1.61.1

      - name: Run desktop and mobile Search phase 3 assertions
        env:
          SEARCH_PHASE3_BASE_URL: http://127.0.0.1:4176
          SEARCH_PHASE3_SOURCE_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          mkdir -p work/search-phase3
          python -m http.server 4176 --directory published/search-phase3 \
            > work/search-phase3/http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" 2>/dev/null || true' EXIT
          for attempt in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:4176/index.html >/dev/null; then
              break
            fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4176/index.html >/dev/null
          node scripts/search_phase3_runtime.js

      - name: Assert browser did not mutate DB
        if: always()
        run: |
          set -euo pipefail
          test -s work/search-phase3-db-before.sha256
          test -s work/search-phase3.sqlite3
          BEFORE=$(cat work/search-phase3-db-before.sha256)
          AFTER=$(sha256sum work/search-phase3.sqlite3 | awk '{print $1}')
          echo "db before=$BEFORE"
          echo "db after =$AFTER"
          test "$BEFORE" = "$AFTER"

      - name: Upload Search phase 3 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: search-phase3-runtime-${{ github.run_id }}
          retention-days: 30
          path: |
            work/search-phase3/search-phase3-*.json
            work/search-phase3/search-phase3-*.png
            work/search-phase3/http.log
            work/search-phase3-db-before.sha256
            published/search-phase3/site-manifest.json
          if-no-files-found: warn

      - name: Publish Search phase 3 runtime conclusion
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ "$JOB_STATUS" = "success" ]; then
            STATE=success
            DESCRIPTION="Search phase 3 runtime succeeded"
          else
            STATE=failure
            DESCRIPTION="Search phase 3 runtime failed"
          fi
          gh api --method POST "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
            -f state="$STATE" \
            -f context=search-phase3-runtime \
            -f description="$DESCRIPTION" \
            -f target_url="$TARGET_URL"
