# 인증키 포함 설정 (실제 값 — 절대 커밋 금지)
config.yaml
.env

# 수집 원본 — Actions Artifact로 보존한다 (명세서 v3.1 §4)
data/raw/
data/inbox/
data/exports/
data/backups/

# 작업 중 DB — publish/ 스냅샷만 배포한다 (명세서 v3.1 §3)
work/
*.sqlite3-wal
*.sqlite3-shm

# 생성 산출물 — rate-data 브랜치로 배포한다
site/
site-public/
publish/

# 파이썬 기본
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
